<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="zombiedition.nikiss.com.gameorange">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppThemeNoBar">
        <activity
            android:name=".Activity.SplashLoadActivity"
            android:configChanges="orientation|keyboardHidden|screenSize"
            android:label="@string/title_activity_index"
            android:theme="@style/FullscreenTheme" />
        <activity
            android:name=".Activity.AcceuilActivity"
            android:screenOrientation="portrait">

        </activity>

        <service
            android:name=".utils.MusicService"
            android:enabled="true" />

        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait" />
        <activity android:name=".Activity.HighScore" />
        <activity android:name=".Activity.Level" />
        <activity
            android:name=".Activity.SplashActivity"
            android:theme="@style/FullscreenTheme" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".Activity.MagasinActivity" />
        <activity android:name=".Activity.OptionsActivity"></activity>
    </application>

</manifest>